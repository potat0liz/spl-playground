# Normalise line endings so the repo is identical on every platform.
* text=auto eol=lf

# PowerShell is Windows-facing; keep CRLF in the working copy.
*.ps1 text eol=crlf

# Never touch these.
*.docx binary
*.pdf  binary
*.png  binary
*.jpg  binary
